# Trained Models

This folder is reserved for the trained machine learning models and
supporting preprocessing objects.

Large model files will not be committed directly if they exceed
GitHub's file size limits.